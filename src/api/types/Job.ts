/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Vectara from "../index.js";

/**
 * A background job for processing long-running operations on the platform.
 */
export interface Job {
    /** The ID of the job. */
    id?: string;
    /** The type of job. */
    type?: Job.Type;
    /** The corpora that this job belongs to. It may not belong to any corpora. */
    corpus_keys?: Vectara.CorpusKey[];
    state?: Vectara.JobState;
    /** Specifies when the job was created. */
    created_at?: string;
    /** Specifies when the job was started. */
    started_at?: string;
    /** Specifies when the job was completed. */
    completed_at?: string;
    /** The username of the user who created the job. This property may be missing, e.g., if the job was created by the system, not a user. */
    created_by_username?: string;
}

export namespace Job {
    /**
     * The type of job.
     */
    export type Type = "rebuild_vector_index" | "replace_filter_attributes" | "unknown";
    export const Type = {
        RebuildVectorIndex: "rebuild_vector_index",
        ReplaceFilterAttributes: "replace_filter_attributes",
        Unknown: "unknown",
    } as const;
}
