/**
 * This file was auto-generated by Fern from our API Definition.
 */

/**
 * A hallucination corrector that detects and corrects hallucinations in generated text.
 */
export interface HallucinationCorrector {
    /** The unique ID for the Hallucination Corrector (HC). */
    id?: string;
    /** The hallucination corrector name, typically including the base name and variant. */
    name?: string;
    /** The hallucination corrector type. Defaults to `vectara`. */
    type?: string;
    /** A short description of the hallucination corrector’s purpose. */
    description?: string;
    /** Indicates whether the hallucination corrector is enabled and available for use in hallucination correction workflows. */
    enabled?: boolean;
}
